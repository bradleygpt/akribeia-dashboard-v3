import { RouteLoading } from "../route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Loading sector analytics"
      detail="Reconciling aggregate scores, dispersion, constituent membership and preserved narratives."
    />
  );
}
