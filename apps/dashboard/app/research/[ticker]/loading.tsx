import { RouteLoading } from "../../route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Opening security intelligence"
      detail="Resolving identity, score pillars, risk, quarterly evidence and pinned V2 detail shards."
    />
  );
}
