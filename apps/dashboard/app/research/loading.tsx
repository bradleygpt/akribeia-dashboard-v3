import { RouteLoading } from "../route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Loading the research universe"
      detail="Preparing all 1,361 securities, preserved V2 filters, comparison state and live research references."
    />
  );
}
