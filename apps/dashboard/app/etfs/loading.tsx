import { RouteLoading } from "../route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Loading the ETF Center"
      detail="Assembling the 70-fund directory, comparisons, templates, holdings and look-through evidence."
    />
  );
}
