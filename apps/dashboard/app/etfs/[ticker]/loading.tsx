import { RouteLoading } from "../../route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Opening ETF intelligence"
      detail="Resolving ETF identity, risk, reference metadata, holdings coverage and look-through scoring."
    />
  );
}
